import math
import math as m # doing this means we just type m whenever we use a math prompt
from math import sqrt, tan # this imports only the sqrt function from the math module, and the tan function

print(math.sqrt(3))
print(m.sqrt(3)) # using the alias m for math
print(sqrt(3)) # using the imported sqrt function directly
print(tan(45)) # using the imported tan function directly